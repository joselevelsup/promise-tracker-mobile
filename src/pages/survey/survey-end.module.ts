import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SurveyEnd } from './survey-end';

@NgModule({
  declarations: [
    SurveyEnd,
  ],
  imports: [
    IonicPageModule.forChild(SurveyEnd),
  ],
})
export class SurveyEndModule {}
